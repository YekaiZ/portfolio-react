export const MobileMotinConsent = ({ handleClick }) => {
  return (
    <>
      <div>Please provide motion access for the full functionality</div>
      <button onClick={handleClick}>OK!</button>
    </>
  );
};
